namespace SimpleStateMachine.StructuralSearch.Rules
{
    public enum RuleOperatorType
    {
        Equals = 0,
        Contains,
        StartsWith,
        EndsWith,
        In,
        Match,
        Is
    }
}